import { MobileCollapseDirective } from './mobile-collapse.directive';

describe('MobileCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
