import { AddTargetDirective } from './add-target.directive';

describe('AddTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTargetDirective();
    expect(directive).toBeTruthy();
  });
});
